#include "Vertex.h"

namespace Mimic
{

	Vertex::Vertex(glm::vec3 position, glm::vec3 normal, glm::vec2 textureCoordinates) : Position(position), Normal(normal), TextureCoordinates(textureCoordinates){ }
}